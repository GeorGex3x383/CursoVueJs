

let firstName;
let lastName = 'Herrera';

//console.log(`${firstName || 'No first name'} ${lastName || 'No last name'}`);

const isActive = true;

const message = (isActive) ? 'Activo' : 'Imnactivo';

console.log(message);

